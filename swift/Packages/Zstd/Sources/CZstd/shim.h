// Umbrella header for the system libzstd + zlib C libraries. Only the
// streaming APIs declared here are used by the Zstd Swift target
// (ZSTD_*Stream* from zstd.h, inflate/deflate from zlib.h).
#ifndef CZSTD_SHIM_H
#define CZSTD_SHIM_H

#include <zstd.h>
#include <zlib.h>

#endif /* CZSTD_SHIM_H */
